services:
  site:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:${SITE_PORT:-8092}:80"
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
      - /tmp
    security_opt:
      - no-new-privileges:true
